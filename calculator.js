export function add(num1, num2) {
    return num1 + num2;
}

export function subtract(subnum1, subnum2) {
    return subnum1 - subnum2;
}

export function multiply(multnum1, multnum2) {
    return multnum1 * multnum2;
}